-- CLASSES
WARRIOR = 1;
PALADIN = 2;
HUNTER = 3;
ROGUE = 4;
PRIEST = 5;
DEATHKNIGHT = 6;
SHAMAN = 7;
MAGE = 8;
WARLOCK = 9;
MONK = 10;
DRUID = 11;
DEMONHUNTER = 12;
EVOKER = 13;

ALL_CLASSES = {
	-- #if AFTER WRATH
	DEATHKNIGHT,
	-- #endif
	-- #if AFTER LEGION
	DEMONHUNTER,
	-- #endif
	DRUID,
	-- #if AFTER DF
	EVOKER,
	-- #endif
	HUNTER,
	MAGE,
	-- #if AFTER MOP
	MONK,
	-- #endif
	PALADIN,
	PRIEST,
	ROGUE,
	SHAMAN,
	WARLOCK,
	WARRIOR,
};
TANKS = {
	-- #if AFTER WRATH
	DEATHKNIGHT,
	-- #endif
	-- #if AFTER LEGION
	DEMONHUNTER,
	-- #endif
	DRUID,
	-- #if AFTER MOP
	MONK,
	-- #endif
	PALADIN,
	WARRIOR,
};
HEALERS = {
	DRUID,
	-- #if AFTER DF
	EVOKER,
	-- #endif
	-- #if AFTER MOP
	MONK,
	-- #endif
	PALADIN,
	PRIEST,
	SHAMAN,
};
CLOTH_CLASSES = {
	MAGE,
	PRIEST,
	WARLOCK,
};
LEATHER_CLASSES = {
	-- #if AFTER LEGION
	DEMONHUNTER,
	-- #endif
	DRUID,
	-- #if AFTER MOP
	MONK,
	-- #endif
	ROGUE,
};
LEATHER_CLASSES_INTELLECT = {
	DRUID,
	-- #if AFTER MOP
	MONK,
	-- #endif
};
MAIL_CLASSES = {
	-- #if AFTER DF
	EVOKER,
	-- #endif
	HUNTER,
	SHAMAN,
};
MAIL_CLASSES_AGILITY = {
	HUNTER,
	SHAMAN,
};
MAIL_CLASSES_HEALER = {
	-- #if AFTER DF
	EVOKER,
	-- #endif
	SHAMAN,
};
PLATE_CLASSES = {
	-- #if AFTER WRATH
	DEATHKNIGHT,
	-- #endif
	PALADIN,
	WARRIOR,
};
PLATE_CLASSES_HEALER = {
	PALADIN,
};
AGILITY_CLASSES = {
	-- #if AFTER LEGION
	DEMONHUNTER,
	-- #endif
	DRUID,
	HUNTER,
	-- #if AFTER MOP
	MONK,
	-- #endif
	ROGUE,
	SHAMAN,
};
STRENGTH_CLASSES = {
	-- #if AFTER WRATH
	DEATHKNIGHT,
	-- #endif
	PALADIN,
	WARRIOR,
};
INTELLECT_CLASSES = {
	DRUID,
	-- #if AFTER DF
	EVOKER,
	-- #endif
	MAGE,
	-- #if AFTER MOP
	MONK,
	-- #endif
	PALADIN,
	PRIEST,
	SHAMAN,
	WARLOCK,
};
EARTHEN_CLASSES = {
	HUNTER,
	MAGE,
	MONK,
	PALADIN,
	PRIEST,
	ROGUE,
	SHAMAN,
	WARLOCK,
	WARRIOR,
};
SWORD_CLASSES = {
	WARRIOR,
	PALADIN,
	ROGUE,
	HUNTER,
	-- #if AFTER WRATH
	DEATHKNIGHT,
	-- #endif
	-- #if AFTER LEGION
	DEMONHUNTER,
	-- #endif
	MAGE,
	-- #if AFTER MOP
	MONK,
	-- #endif
	WARLOCK
};